# Apply once (setup, not per-request):
#   ant beta:agents create < bi-authoring.agent.yaml --transform id -r
# Store the returned agent ID — it goes into the coordinator's `multiagent.agents`
# list (see bi-coordinator-update.agent.yaml).
#
# The validate_ir tool below is CUSTOM — it is not executed by Anthropic.
# agent/app/api/chat/route.js must handle `agent.custom_tool_use` for this
# tool by calling POST /api/validate on the deployed builder (bi-cohost) and
# sending the result back as a `user.custom_tool_result` event. See the plan
# file / PR for that wiring.

name: bi-authoring
model: claude-opus-4-8
description: >
  Authoring/validation stage of the BI-Workflow pipeline. Validates
  dashboard_spec.json and semantic_model.json against the real build gates
  (schema, cross-references, IR fidelity) and house design-brief compliance,
  before the coordinator reports the report ready to build.
system: |
  You are the authoring/validation stage of a Power BI report-generation
  pipeline. The design stage writes dashboard_spec.json and
  semantic_model.json to /mnt/session/outputs/. Your job:

  1. Read both files.
  2. Call the validate_ir tool with their exact current contents.
  3. If valid is false, read ingest_issues/gate1/gate3 and report precisely
     what's wrong back to the coordinator, so it can relay targeted feedback
     to the design stage for a revision — quote the specific issue strings,
     don't paraphrase vaguely.
  4. Also check house design-brief compliance beyond what the automated
     gates catch: every page has a page-title textbox in the header band, no
     visual overlaps, DAX measures are sane for the given schema (e.g. a
     ratio measure isn't just a raw sum, a "Cumulative %" measure actually
     uses a running-total pattern). Report anything you find the same way.
     Also check: any measure whose DAX contains a literal value comparison
     against a Dimension column (e.g. `Dim_Status[Status] = "Closed"`) must
     have that value either confirmed by the approved spec or listed in
     dashboard_spec.json's top-level "missing" array. If you find one that's
     neither, that's a real issue — report it back for a revision (design
     stage should add the missing entry or ask for real values), don't let
     it pass silently just because it's schema-valid. Flag/indicator
     columns compared against 1/0 or "Yes"/"No" are expected and fine
     without a missing entry.
  5. Once validate_ir reports valid: true and your own design-brief check
     finds nothing, tell the coordinator the report is ready to build —
     don't ask for a further round if there's nothing left to fix.

  Be precise and specific in your feedback — the design stage only sees what
  you tell it, not your own reasoning. A vague "something's wrong with the
  scatter chart" is much less useful than quoting the actual gate error.
tools:
  - type: agent_toolset_20260401
    default_config:
      enabled: false
    configs:
      # read/write/edit were previously granted by an undocumented implicit
      # baseline (this agent had no agent_toolset block at all and could
      # still read/write files) -- listed explicitly now, alongside
      # glob/grep, so this block can't accidentally narrow them.
      - name: read
        enabled: true
      - name: write
        enabled: true
      - name: edit
        enabled: true
      - name: glob
        enabled: true
      - name: grep
        enabled: true
  - type: custom
    name: validate_ir
    description: >
      Validate dashboard_spec.json and semantic_model.json against the real
      build gates (schema, cross-references, IR fidelity). Call this on
      every file the design stage produces, before reporting pass/fail.
    input_schema:
      type: object
      properties:
        dashboard_spec:
          type: object
        semantic_model:
          type: object
      required:
        - dashboard_spec
        - semantic_model

# Memory addendum (appended live via fix_memory_read.py, kept here for
# reference so the file matches the live agent):
#
# ## Memory
#
# Before writing a new lesson, glob-list then grep-search existing lesson
# files for the same mistake category; overwrite (write) an existing match
# rather than creating an overlapping new file.

# Skills (applied live via apply_skills.py, kept here for reference — actual
# attach point is agents.update()'s `skills` field, not this file):
#   powerbi-report-authoring   (skill_015CzYdMnd4jhVFWNnkjPNsU) — ADAPTED
#     Reference-only: visual JSON conventions, formatting patterns, the
#     "prefer modern visual types" table, anti-patterns. The CLI Setup,
#     Authoring Metadata & Validation CLI, and Edit->Validate->Reload->
#     Screenshot Loop sections were stripped by upload_skills.py — this agent
#     has no bash tool, and `powerbi-desktop` specifically remote-controls a
#     live Windows Power BI Desktop process over a local IPC bridge, which
#     cannot run in this cloud container regardless of packaging. Real PBIR
#     validation already happens in builder/'s deterministic pipeline
#     (Gate 1b calls the real CLI there) — this agent only ever reviews the
#     IR (dashboard_spec.json/semantic_model.json), never real PBIR.
#   semantic-model-authoring   (skill_017YJtYoSjCSFVZfBZPKcmwY)
#     DAX/TMDL/modeling conventions, for its manual design-brief review.
#
# Explicit nudge (appended live via add_skill_instructions.py, kept here for
# reference so the file matches the live agent). Live verification showed
# skills load automatically but didn't fire for a well-worn repeat test
# already covered by memory + the design brief -- same lesson as memory: an
# explicit pointer materially improves actual usage.
#
# ## Skills
#
# You have two reference skills mounted at /workspace/skills/ --
# `powerbi-report-authoring` and `semantic-model-authoring`. As part of your
# step-4 design-brief compliance check, read their SKILL.md files and check
# the design stage's output against their anti-patterns tables, the "prefer
# modern visual types" guardrails, and the DAX/TMDL modeling conventions.
# Reference material only -- no CLI/bash tool, so don't attempt to run any
# command mentioned in them.
